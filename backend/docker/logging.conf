# Docker Logging Configuration

# Environment variables for conversation logging
BOT_LOG_LEVEL=INFO
BOT_LOG_FORMAT=json

# Enable file logging in production, but use stdout in development
# for easier integration with Docker log drivers
BOT_FILE_LOGGING=false

# Log directory inside the container
BOT_LOG_DIR=/app/logs